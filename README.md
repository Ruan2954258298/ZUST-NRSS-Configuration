# ZUST-NRSS Configuration

配置库目录结构依据项目配置管理要求初始化。

## 版本发布约定

- 使用语义化版本号（SemVer）：`主版本.次版本.修订版本`。
- 当前基线版本：`v1.0.0`（仓库初始化版本）。
- 后续小幅文档或配置修正：递增修订号（例如 `v1.0.1`）。

## Release 对应关系

- `v1.0.0`：初始化配置库目录结构。
- `v1.0.1`：补充版本发布与变更记录文档。
